apply plugin: 'checkstyle'

checkstyle {
    toolVersion = '6.14.1'
    configFile file("${project.rootDir}/quality/checkstyle.xml")
    configProperties = [
            'checkstyle.cache.file': file("${project.rootDir}/checkstyle.cache")
    ]

    sourceSets = [sourceSets.main]
}